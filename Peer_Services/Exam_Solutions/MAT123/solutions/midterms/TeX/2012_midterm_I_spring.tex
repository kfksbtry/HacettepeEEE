\documentclass[a4paper, 12pt]{article}

% Essential Packages
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[top=25mm, bottom=25mm, left=25mm, right=25mm]{geometry}
\usepackage{pgfplots}
\usepackage[inline]{enumitem}
\usepackage{mathtools}
\usepackage{tikz}
\usepackage{fancyhdr}
\usepackage{setspace}
\usepackage{hyperref}

% Misc
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{polar}
\usepgfplotslibrary{fillbetween}
\setlength{\parskip}{3pt}
\setstretch{1.15}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyhf{}

\begin{document}
\pagestyle{empty}

\begin{center}
\textbf{2011-2012 Spring \\MAT123 Midterm I\\06/04/2012\\Duration: 105 minutes}
\end{center}

\begin{enumerate}[leftmargin=*,label=\textbf{\arabic*.}]

\item Find the equation of the tangent line to the curve $x^2+2xy+y^2=4$ at the point $(3,-1)$.

\item Find the point on the parabola $y=\sqrt x$ which is the closest to the point $(2,0)$.

\item Evaluate the limit, if it exists, and explain your answer. Do not use L'Hôpital's rule.

\begin{enumerate}[leftmargin=*,label=\textbf{\alph*.}]
\item $\displaystyle\lim_{x\to0}\sqrt{x^2+2x^3} \sin\left(\frac1x\right)$ \ \ \ \item $\displaystyle \lim_{x\to3}\frac{x^2-9}{x^2-x-6}$ \ \ \ \item $\displaystyle \lim_{x\to-\infty}\sqrt{x^2-4x}+x$

\item $\displaystyle\lim_{h\to0}\frac{(1+h)^{123}-1}{h}$
\end{enumerate}

\item Find the derivatives of the following functions.

\begin{enumerate}[leftmargin=*,label=\textbf{\alph*.}]
\item $f(x)=x^{\cos\left(x^3\right)}$ \ \ \ \item $f(x) = \tan\left( e^{2x}\sin(3x)\right)$

\item Find the second derivative $f''(x)$ of $\displaystyle f(x) =\ln\left(\frac{x^2}{x^2+4}\right)$.
\end{enumerate}

\item For the function $\displaystyle f(x)=\frac{1}{x^2-4}$,

\begin{enumerate}[leftmargin=*,label=\textbf{\alph*.}]
\item Find the vertical and horizontal asymptotes.
\item Find the intervals of increase or decrease.
\item Find the local maximum and minimum values, if any.
\item Find the intervals of concavity and the inflection points, if any.
\item Sketch the graph of $f$.
\end{enumerate}

\end{enumerate}

\newpage
\pagestyle{fancy}
\renewcommand{\headrulewidth}{1pt}
\fancyhead[R]{\textit{2011-2012 Spring Midterm I}}
\fancyhead[L]{\textit{Hacettepe MAT123 Exam Solutions Version 2}}

% Last update: 29/08/2025 19:24)

\begin{enumerate}[leftmargin=*,label=\textbf{\arabic*.}]

\item $y$ is implicitly defined as a function of $x$. Differentiate each side.
\[\frac{d}{dx}\left(x^2+2xy+y^2\right)=\frac{d}{dx}\,(4)\]
\[2x+2y\cdot1+2x\cdot\frac{dy}{dx}+2y\frac{dy}{dx}=0\]
\[\frac{dy}{dx}(2x+2y)=-2x-2y\]
\[\frac{dy}{dx}=-1\]

Recall the equation of a straight line: $y-y_0=m(x-x_0)$, where $m$ is simply $\displaystyle \frac{dy}{dx}$. Therefore, the tangent line at $(3,-1)$ is as follows.
\[\boxed{y+1=-1(x-3)}\]

Furthermore, since $\displaystyle \frac{dy}{dx}=-1$, the tangent line consists of every point of the upper line. In other words, the tangent line is the upper line itself. The equation $x^2+2xy+y^2=4$ forms two parallel straight lines in the $xy-$coordinate system.

\vspace{1em}

\item Let $\left(x,\sqrt x\right)$ be a point on this parabola. The distance between the points can be expressed using the Pythagorean theorem as follows.
\[f(x)=(2-x)^2 + \left(\sqrt x - 0\right)^2=L^2\]

Take the derivative of both sides and set $\displaystyle f'(x)=0$ to find the critical points.
\begin{equation*}
f'(x)=2(2-x)\cdot(-1) + 1 = 2x-3=0 \implies x=\frac{3}2
\end{equation*}

Now, verify whether this is a local minimum by taking the second derivative.
\[f''(x)=(2x-3)'=2>0\]

Since this is a local minimum of $f$, the distance is closest at $\displaystyle x=\frac32$. The point we are looking for is
\[\boxed{\left(\frac32,\sqrt{\frac32}\right)}\]

\newpage

\item The inequality $\displaystyle-1 \leq \sin\left(\frac1x\right)\leq1$ holds for all $x\in\mathbb{R}$ except for $x=0$. For small $x$, we can multiply each side of the inequality by $\sqrt{x^2+2x^3}$. Using the squeeze theorem, the limit is equal to $0$.

\begin{enumerate}[leftmargin=*,label=\textbf{\alph*.}]
\item The inequality $\displaystyle-1 \leq \sin\left(\frac1x\right)\leq1$ holds for all $x\in\mathbb{R}$ except for $x=0$. For small $x$, we can multiply each side of the inequality by $\sqrt{x^2+2x^3}$. Using the squeeze theorem, the limit is equal to $0$.
\[-1\leq\sin\left(\frac1x\right)\leq1\implies -\sqrt{x^2+2x^3}\leq\sqrt{x^2+2x^3}\sin\left(\frac1x\right)\leq\sqrt{x^2+2x^3}\]
\[\lim_{x\to0}-\sqrt{x^2+2x^3}=\lim_{x\to0}\sqrt{x^2+2x^3}=0\implies\lim_{x\to0}\sqrt{x^2+2x^3}\sin\left(\frac1x\right)=\boxed0\]

\item Factor each side of the fraction and eliminate like terms.
\[\lim_{x\to3}\frac{x^2-9}{x^2-x-6}=\lim_{x\to3}\frac{(x-3)(x+3)}{(x-3)(x+2)}=\lim_{x\to3}\frac{x+3}{x+2}=\boxed{\frac65}\]

\item The expression is in the form $\infty-\infty$. Expand the expression by multiplying by its conjugate to eliminate the indetermination.
\begin{align*}
\lim_{x\to-\infty}\sqrt{x^2-4x}+x&=\lim_{x\to-\infty}\left[\left(\sqrt{x^2-4x} + x\right) \cdot\frac{\sqrt{x^2-4x}-x}{\sqrt{x^2-4x}-x}\right]\\[1em]&=\lim_{x\to-\infty}\frac{x^2-4x-x^2}{\sqrt{x^2-4x}-x}\\[1em]&=\lim_{x\to-\infty}\frac{-4x}{\sqrt{x^2-4x}-x}=\lim_{x\to-\infty}\frac{-4x}{\displaystyle |x|\sqrt{1-\frac4x}-x}\\[1em]&=\lim_{x\to-\infty}\frac{-4}{\displaystyle-\sqrt{1-\frac4x}-1}=\lim_{x\to-\infty}\frac42=\boxed{2}\end{align*}

\item Recall the definition of the derivative of a function at a point. Let $f$ be a differentiable function, then
\[\lim_{h\to 0} \frac{f(x+h)-f(x)}{h} = f'(x)\]

If we set $f(x) = x^{123}$, then we can differentiate $f$ at $x=1$.
\[\lim_{h\to0}\frac{(1+h)^{123}-1}{h}=f'(1)=123\cdot(1)^{122} = \boxed{123}\]
\end{enumerate}

\item Take the logarithm of each side to compute the derivative easily.
\begin{enumerate}[leftmargin=*,label=\textbf{\alph*.}]
\item Take the logarithm of each side to compute the derivative easily.
\[f(x)=x^{\cos\left(x^3\right)}\]
\[\ln(f(x))=\ln\left[x^{\cos\left(x^3\right)}\right]=\cos\left(x^3\right)\cdot\ln x\]
\[\frac{d}{dx}\left[\ln(f(x))\right] = \frac{d}{dx}\left[\cos\left(x^3\right)\cdot\ln x\right]\]
\[\frac{1}{f(x)}\cdot f'(x) = -\sin\left(x^3\right)\cdot 3x^2 \cdot \ln x + \cos\left(x^3\right)\cdot\frac1x\]
\[\boxed{f'(x) = x^{\cos\left(x^3\right)}\cdot\left[-\sin\left(x^3\right)\cdot 3x^2 \cdot \ln x + \cos\left(x^3\right)\cdot\frac1x\right]}\]

\item Apply the chain rule accordingly.
\begin{equation*}f(x) = \tan\left( e^{2x}\sin(3x)\right)\end{equation*}
\begin{equation*}\boxed{f'(x) = \sec^2\left( e^{2x}\sin(3x)\right)\cdot\left[ e^{2x}\cdot 2\cdot\sin(3x) +  e^{2x}\cdot\cos(3x)\cdot3\right]}\end{equation*}

\item Compute the first and second derivatives, respectively, applying the chain rule and the quotient rule accordingly.
\begin{equation*}f(x) = \ln\left(\frac{x^2}{x^2+4}\right)\end{equation*}
\begin{equation*}f'(x)= \frac{x^2+4}{x^2}\cdot\frac{2x\cdot(x^2+4)-x^2\cdot2x}{\left(x^2+4\right)^2}=\frac{8}{x^3+4x}\end{equation*}
\begin{equation*}f''(x)= -\frac{8}{\left(x^3+4x\right)^2}\cdot\left(3x^2+4\right)=\boxed{-\frac{24x^2+32}{\left(x^3+4x\right)^2}}\end{equation*}
\end{enumerate}

\item Find the horizontal asymptotes.
\begin{enumerate}[leftmargin=*,label=\textbf{\alph*.}]
\item Find the horizontal asymptotes.
\[\lim_{x\to\pm\infty}\frac{1}{x^2-4}=0\]

Find the vertical asymptotes. The expression is undefined for $x=\pm2$.
\[\lim_{x\to2^+}\frac{1}{x^2-4}=\lim_{x\to-2^-}\frac{1}{x^2-4}=\infty,\qquad \lim_{x\to2^-}\frac{1}{x^2-4}=\lim_{x\to-2^+}\frac{1}{x^2-4}=-\infty\]
\[\boxed{\text{The horizontal asymptote is } y= 0. \text{ The vertical asymptotes are } x=\pm2.}\]

\item Compute the first derivative and set it to $0$ to find the critical points.
\[f'(x)=-\frac{1}{\left(x^2-4\right)^2}\cdot2x\]
$f$ is increasing where $f'(x)>0$ and decreasing where $f'(x)<0$.
\[\boxed{\text{f is decreasing for } x>0, \text{increasing for } x<0.}\]

\item The \textit{only} critical point occurs at $x=0$. Compute the second derivative to check whether this is a local minimum or local maximum.
\begin{equation*}f''(x)=-\frac{2\cdot\left(x^2-4\right)^2-2x\cdot2\cdot(x^2-4)\cdot(2x)}{\left(x^2-4\right)^4}=\frac{8+6x^2}{\left(x^2-4\right)^3}\end{equation*}
$\displaystyle f''(0)=\frac{8+6\cdot0^2}{\left(0^2-4\right)^3}=-\frac18<0$. Therefore, $(0, f(0))$  is a local maximum.

\[
\boxed{
\begin{array}{c}
\displaystyle \text{No local minimums exist.}\\\displaystyle \text{The }\textit{only} \text{ local maximum occurs at } x=0,\text{ which is}\left(0,-\frac14\right).
\end{array}}\]

\item $8+6x^2 \geq 0$. Therefore, no inflection points. $f$ is concave up if $f''(x)>0$, concave down if $f''(x)<0$.
\[
\boxed{
\begin{array}{c}
\displaystyle \text{No inflection points exist. }\\$f$ \text{ is concave up for } x>2 \text{ and } x<-2.\:$f$ \text{ is concave down for } |x|<2.
\end{array}}\]

\item The graph is shown below.
\begin{center}
\begin{tikzpicture}
\begin{axis}[
    domain=-6:6,
    ymin=-4, ymax=4,
    samples=500,
    axis lines=middle,
    xlabel={$x$},
    ylabel={$y$},
    restrict y to domain=-4:4,
    enlargelimits=true,
    grid=none,
    xtick={-6,-4,-2,0,2,4,6},
    ytick={-6,-4,-2,0,2,4,6},
    unbounded coords=jump,
    clip=true,
    scale=1.6
]
\addplot[blue, thick] {1/(x^2 - 4)};
\draw[dashed, red] (axis cs: -6,0.02) -- (axis cs: 6,0.02);
\draw[dashed, red] (axis cs: 2,-4) -- (axis cs: 2,4);
\draw[dashed, red] (axis cs: -2,-4) -- (axis cs: -2,4);
\end{axis}
\end{tikzpicture}
\end{center}
\end{enumerate}
\end{enumerate}

\end{document}